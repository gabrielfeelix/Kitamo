# Copie para .hostinger.deploy.env (arquivo ignorado pelo git)
# cp .hostinger.deploy.env.example .hostinger.deploy.env

# Opcao 1 (recomendada): use alias SSH do ~/.ssh/config
SSH_TARGET=hostinger-kitamo

# Opcao 2: host + user + porta
# SSH_HOST=YOUR_HOST_OR_IP
# SSH_USER=YOUR_SSH_USER
# SSH_PORT=22

# Caminho do projeto no servidor
PROJECT_DIR=~/domains/your-domain/public_html

# Chave SSH local (opcional)
SSH_KEY=~/.ssh/kitamo_deploy
